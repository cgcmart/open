<%= render 'spree/admin/shared/configuration_menu' %>

<% admin_breadcrumb(link_to(plural_resource_name(Spree::Store), admin_stores_path)) %>
<% admin_breadcrumb(t('spree.new_store')) %>

<% content_for :page_actions do %>
<% end %>

<%= form_for [:admin, @store] do |f| %>
  <%= render 'form', f: f %>

  <% if can? :create, @store %>
    <%= render 'spree/admin/shared/new_resource_links', locals: { collection_url: spree.admin_stores_path(@store) } %>
  <% end %>
<% end %>
